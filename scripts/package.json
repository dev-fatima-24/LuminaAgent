{
  "name": "lumina-agent-scripts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "ts-node deploy.ts",
    "init": "ts-node init-wallet.ts",
    "add-rules": "ts-node add-rules.ts",
    "execute": "ts-node execute-rules.ts",
    "logs": "ts-node fetch-logs.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
